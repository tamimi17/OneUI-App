<?xml version="1.0" encoding="utf-8"?>
<!--
    fragment_favorite_font_list.xml
    Layout قائمة المفضلة — مطابق لـ fragment_local_font_list.xml
    مع حذف select_folder_container لأن المفضلة لا تحتاج لاختيار مجلد.

    يحتوي على:
    - main_content_layout: يُعرض عند وجود خطوط مفضلة (RecyclerView + Empty View)
    - الحالة الفارغة (empty_view): تظهر عند عدم وجود خطوط مفضلة بعد

    ★ التحكم في الشاشة الفارغة من هنا مباشرةً:
      - empty_title  → عنوان الحالة الفارغة  (غيّر textSize أو textColor هنا)
      - empty_text   → وصف الحالة الفارغة   (غيّر textSize أو textColor هنا)
        ملاحظة: نص empty_text يُعيَّن برمجياً من FontUIStateManager
        (يتغير تلقائياً إلى "لا توجد نتائج" عند البحث)
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/main_content_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="visible">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/font_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="vertical" />

        <androidx.core.widget.NestedScrollView
            android:id="@+id/empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true"
            android:visibility="gone"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center"
                android:padding="10dp"
                android:minHeight="400dp"
                android:layout_gravity="center">

                <!-- أيقونة الحالة الفارغة: نجمة المفضلة بشفافية -->
                <androidx.appcompat.widget.AppCompatImageView
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:layout_marginBottom="20dp"
                    android:src="@drawable/ic_favorite"
                    android:alpha="0.4" />

                <!-- عنوان الحالة الفارغة: يُعيَّن مباشرةً من XML
                     ★ للتخصيص: غيّر textSize أو textColor في هذا الملف مباشرةً ★ -->
                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/empty_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:layout_marginBottom="15dp"
                    android:text="@string/favorites_empty_title"
                    android:textSize="17sp"
                    android:textColor="@color/sesl_description_text_color" />

                <!-- وصف الحالة الفارغة: يُعيَّن برمجياً من FontUIStateManager
                     (يتغير إلى "لا توجد نتائج" عند تفعيل البحث)
                     ★ للتخصيص: غيّر textSize أو textColor في هذا الملف مباشرةً ★ -->
                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/empty_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:lineSpacingExtra="5dp"
                    android:text="@string/favorites_empty_message"
                    android:textSize="14sp"
                    android:textColor="?android:attr/textColorSecondary" />

            </LinearLayout>

        </androidx.core.widget.NestedScrollView>

    </LinearLayout>

</FrameLayout>
